"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.not = exports.or = exports.and = void 0;
const and = (...filters) => (value, index, array) => filters.reduce((prev, filter) => prev && filter(value, index, array), true);
exports.and = and;
const or = (...filters) => (value, index, array) => filters.reduce((prev, filter) => prev || filter(value, index, array), false);
exports.or = or;
const not = (filterFn) => (value, i, array) => !filterFn(value, i, array);
exports.not = not;
