"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const uvu_1 = require("uvu");
const assert = __importStar(require("uvu/assert"));
const filter_1 = require("./filter");
const test = uvu_1.suite('filter');
// filter ---------------------------------------------------------------------------------------------------
test(`filter for 1`, () => {
    const items = [1, 2, 3, 5, 8, 1];
    const filterFor1 = filter_1.createFilter((item) => item === 1);
    const filteredItems = items.filter(filterFor1);
    assert.ok(filteredItems.length === 2);
});
test(`filter for id not null`, () => {
    const item1 = { id: 1 };
    const item2 = { id: null };
    const items = [item1, item2, item1];
    const filterForIdNotNull = filter_1.createFilter((item) => item.id !== null);
    const filteredItems = items.filter(filterForIdNotNull);
    assert.ok(filteredItems.length === 2);
    assert.ok(filteredItems.includes(item1));
});
test.run();
