import { TypeGuard } from '../types';
export declare const isObject: <T>(value: T) => value is TypeGuard<object, T>;
export declare const isPrimitiveObject: <T>(value: T) => value is TypeGuard<object, T>;
