import { BaseTranslation } from '../../../core/src/core';
import type { GeneratorConfigWithDefaultValues } from '../generate-files';
import { Logger } from '../generator-util';
declare type GenerateTypesType = GeneratorConfigWithDefaultValues & {
    translations: BaseTranslation;
};
export declare const generateTypes: (config: GenerateTypesType, logger: Logger) => Promise<boolean>;
export {};
